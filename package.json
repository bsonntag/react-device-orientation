{
  "name": "react-device-orientation",
  "version": "0.1.0",
  "description": "A react wrapper for device orientation events.",
  "keywords": [
    "device",
    "orientation",
    "react"
  ],
  "repository": "git@github.com:bsonntag/react-device-orientation.git",
  "license": "MIT",
  "author": "Benjamim Sonntag <benjamimsonntag@gmail.com>",
  "main": "dist/index.js",
  "scripts": {
    "bundle": "rm -rf dist && babel src --out-dir dist",
    "bundle:watch": "babel src --out-dir dist --watch",
    "lint": "eslint .",
    "version": "npm run bundle && git add dist"
  },
  "dependencies": {
    "prop-types": "^15.7.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.7.0",
    "@babel/core": "^7.7.2",
    "@babel/plugin-proposal-class-properties": "^7.7.0",
    "@babel/preset-env": "^7.7.1",
    "@babel/preset-react": "^7.7.0",
    "@bsonntag/eslint-config": "^0.5.0",
    "eslint": "^6.6.0",
    "husky": "^3.0.9",
    "lint-staged": "^9.4.2",
    "prettier": "^1.18.2"
  },
  "peerDependencies": {
    "react": "^16.3.0"
  }
}
